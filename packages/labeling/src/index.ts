/**
 * @rads/labeling — 2D label placement with leaders and overlap avoidance.
 * Pure functions; no DOM, no three.js. Drives both the WebGL overlay (screen
 * space) and the PDF drawing sheets (paper space).
 */
export * from "./place";

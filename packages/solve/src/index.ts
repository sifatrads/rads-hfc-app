/**
 * @rads/solve — whole-project hydraulic solve: ProjectModel → GGA network
 * (with supply-curve coupling) → per-node/per-pipe results + a Ch.27 summary.
 * Bridges the calc engine to the scene/viewer/report (produces SceneResults).
 */
export * from "./solve";

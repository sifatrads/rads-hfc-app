/**
 * @rads/nfpa170 — fire-safety symbols (NFPA 170-2015 Table 7.2 & 7.6.2) as
 * renderer-agnostic primitives + an SVG renderer + an auto-legend. Used by the
 * report drawing sheets (SVG / @react-pdf) and the 3D viewer (sprite atlas).
 */
export * from "./symbols";
export * from "./render";
